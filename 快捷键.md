# 快捷键

## Mac

**⌘+w: 关闭当前窗口**

**⌘ + ~ : 同一应用内循环切换其打开的所有独立窗口**

**Control-⌘-Q**：锁定屏幕。

 `Option` + `Command` + `C` : 可复制文件完整路径

control + F3 : 聚焦到程序坞,按 → 选中程序

**⇧+⌘ +3: 全屏截图**

## 访达

**⌘ + shift + .** 访达显示隐藏文件

**⌘+N:新建访达窗口**

## Cursor+vscode

Agents和Editor切换: **⌘ + E**

打开最近使用文件: control + R

## mac命令行

```jsx
查看IPv4地址:  ifconfig | grep "inet " | grep -v 127.0.0.1
```